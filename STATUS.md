# Product KB / Workbench V0.8 状态

日期：2026-09-11

## 快速迭代工作流

- 新项目默认使用三段 Fast Iteration Path；完整研究仍可切换。
- 默认交付物为 F1 新品需求分析、F2 核心 PRD、F3 开发验收清单。
- 项目增加目标平台、团队资源和验证周期。
- 竞品评论 CSV、政策、技术日志、截图和研究资料进入主交付物；截图可作为 Codex 图片输入。
- 快速模式不生成用户访谈、假设验证、风险清单、技术 Spike、Gate 或条件闭环。
- F3 在同一份文档中更新计划与实测，不再拆成 Plan/Run。
- F1 恢复 Heart Rate 的完整产品分析结构，建议 12,000–16,000 中文字符；F2 / F3 分别保持 8,000 / 5,000 字符预算，并禁止重复上游内容或加入工作流审计。
- F1 已移除平台、权限、数据与政策独立章节；具体权限交互只在 F2 对应页面需要时说明。
- 评论独立分析改为可选；桌面爬虫自动适配器仍待后置。
- 工作台与 lifecycle、new-product workflow、Product KB 方法和模板已经同步。
- 交付物支持直接编辑 Markdown、填写修改说明、查看版本记录和恢复旧版；每次修改追加新版本。
- 交付物支持对章节、段落、引用、表格和代码块添加定位批注，记录新增/修改/删除/压缩/保留动作，并只按当前版本待处理批注准备修订任务。
- 批注与文档版本绑定，不进入正文；修订版成功生成后，参与修订的批注自动关闭，旧版本批注不会驱动新版本修订。
- 已确认交付物编辑后自动回到待评审，历史版本不进入项目 RAG。
- 未解决批注、断裂来源编号和快速模式未闭环的 Conditional F3 会阻止确认。
- 项目支持输入完整名称后移入回收站，并可从界面恢复；永久删除需要再次输入完整项目名。
- 已取消、执行失败和服务重启中断的任务保留输入快照和任务包，可恢复为待执行。
- 任务记录生成时模式、修订父子关系和来源版本；支持最新版筛选、状态筛选和非破坏归档。
- 引用已有交付物时展开底层来源、统一重新编号，并在生成和编辑时校验 `[Sx]` 闭合。
- 工作台已接入双索引 Hybrid RAG：项目证据优先，全局 active 方法知识补充；异常时自动回退关键词。
- 项目向量使用项目级命名空间隔离；项目移入回收站或永久删除时同步清理向量与 manifest 记录。
- 检索页面继续展示来源和原文，便于人工核对；内部向量 ID、得分和治理字段不会进入任务正文。

## 本地工作台首版

- 新增 `workbench/` 可运行工作台，默认复用当前 Product KB 的 F1–F3 中文模板；历史 T1–T4 仅保留给显式完整研究模式。
- 项目持久化、三层上下文、文本/图片资料导入、项目隔离检索、任务输入快照、Codex 生成、Markdown 编辑与导出、定位批注、版本恢复、评审保护、修订链和项目回收站已实现。
- 能力包通过 JSON 定义加载；评论分析可启用，自动爬取仍未接入。
- 工作台检索已组合项目隔离索引与 Product KB 全局只读索引；原有 MCP 使用方式不变。
- 使用方式与功能边界见 `workbench/README.md`；设计参考见 `workbench/DESIGN.md`；验收记录见 `workbench/VERIFICATION.md`。
- 本机项目资料、工作台数据库及运行记录不进入 Git。

## 已完成（含保留的历史完整版能力）

- 本地 Markdown 知识库目录与 YAML Schema。
- 当前 A0 项目清单、F1–F3 主交付物、A5 原型交接和知识候选模板；旧 A1–A4/A6–A7 已归档。
- 外部来源白名单与敏感信息边界。
- Markdown、TXT、文本型 PDF 解析。
- 标题语义切块和稳定 Chunk ID。
- Qdrant Local dense + sparse Hybrid Search。
- 结构化知识优先、来源去重和查询覆盖重排。
- 文件指纹和增量索引。
- `kb_search`、`kb_get`、`kb_trace`、`kb_find_similar_cases` 四个只读 MCP 工具。
- 42 个代码测试全部通过。
- 30 个 RAG 回归问题全部通过。
- MCP stdio 真实进程冒烟测试通过。
- `product-knowledge-rag` Skill 包创建并校验通过。
- `overseas-consumer-app-lifecycle` Skill 包创建并校验通过。
- Claude 项目级 `.mcp.example.json` 和 Codex 配置示例。
- Heart Rate 第一次工作流回归通过。
- PDF Reader 第二次跨品类回归通过，6 项验收标准全部 PASS；当前阶段为 `OPPORTUNITY`，G1 结论为 `HOLD`。
- Plant Identifier / Plant Care 第三次跨品类回归完成：通用识别方向不建议做，Plant Rescue AI 候选楔子为 `G1 = HOLD`，等待用户、复访和真实照片 Spike。
- 第三次回归验证了“无直接案例时不伪造答案”；两个弱相似案例仅迁移方法，没有迁移产品结论。
- `product-knowledge-rag` 增加默认调用预算，修复单次机会判断过度检索问题。
- MCP 冒烟增加 search → get 往返一致性校验。
- `kb_find_similar_cases` 已强制只返回 `case` 类型，并增加防御性过滤与回归测试。
- MCP 已改为每进程私有 Qdrant 快照，3 个并发客户端冒烟测试通过，无本地索引锁冲突。
- 知识条目增加事实时效字段，明确区分评审日期、事实观察日期、有效期和是否要求实时刷新。
- Product KB MCP 与两个 Skill 已安装到用户级 Codex 配置，并完成安装副本一致性校验。
- Codex CLI 已升级到 0.153.4；使用默认 `gpt-5.6-sol` 原生直连 `kb_search` 成功，未使用 GPT-6。
- GitHub 交付内容已改为 portable examples；本机来源路径、索引和客户端配置不入库。
- 后续正式 Agent 回归使用任务当前默认模型，不锁定特定型号。
- 已整理《出海 C 端新品工作流使用手册》，明确默认 F1–F3 交付物、证据附件、历史正式模式和最小交付标准。
- 工作流已调整为“交付物优先”：用户指定要做的品类默认产出 F1 新品需求分析、F2 核心 PRD 和 F3 开发验收清单；只有明确要求历史 0–12 章或正式大型研究包时才进入 T1–T4。
- 用户指定的品类默认视为要做；目标用户可依据竞品场景和现有资料直接描述，不再要求访谈或形式化假设验证。
- 桌面版“竞品信号工坊”已定义为可选竞品证据采集环节：默认只抓取和核验评论，不调用 DeepSeek，不自动写入 Product KB。
- 已将当前模板统一为 `template-f1-new-product-analysis`、`template-f2-core-prd` 和 `template-f3-development-acceptance`，并同步 Router、生命周期 Skill 和使用手册。
- 已清理新品分析模板中的付费投放、媒体采买、投放归因、ASO 文案、商店素材、商店发布，以及独立的平台、权限、数据和政策章节；具体权限交互只在 F2 对应页面需要时说明。
- 已激活当前新品工作流 Skill 作为本地 RAG 来源；历史 T1–T4、旧 A 模板和历史方案已归档并从默认检索排除。
- 默认检索增加 `status=active` 最终防御过滤，避免 `archived` 或 `superseded` 条目从向量候选中漏出。

## 当前索引

- 发现来源：38 个。
- 有效 active 来源：20 个。
- 历史 archived 来源：14 个；superseded 来源：4 个（保留但不参与正常检索）。
- 索引切块：476 个。
- RAG 测试：30/30。

## 尚未执行

- 尚未建立正式知识候选的人工审核命令。
- Claude 仅保留项目级配置示例，未安装到 Claude 全局目录。

## 下一阶段

1. 建立知识候选的人工审核命令，形成“提取—审核—发布—重建索引”闭环。
2. 根据三次回归结果定义并发布 V1.0。
3. 在第一个正式新品项目中使用 V1.0，并记录三份交付物的生成时间、内容重复率和研发返工率。
